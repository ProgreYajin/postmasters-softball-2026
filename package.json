{
  "name": "r2-upload-worker",
  "version": "1.0.0",
  "description": "LINE Bot image upload worker for Cloudflare R2",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}